import LocalizedStrings from 'react-native-localization';
import english from './en';
import french from './fr';

const languageConfig = new LocalizedStrings({
    //"fr": french,
    "en": english
});

export default languageConfig;